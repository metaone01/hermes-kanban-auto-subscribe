[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --import-mode=importlib
# The repo root is a Hermes plugin *package* (__init__.py + register()), not a
# test module. importlib import-mode stops pytest walking up into it.
norecursedirs = .git __pycache__
